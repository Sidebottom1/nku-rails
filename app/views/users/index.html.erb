
<%- if current_user.present? -%>
  <%= link_to 'Seating Chart', users_path %>
  <%= link_to 'All Attendances', attendances_path %>
  <%= link_to 'Student Index', logout_path %>
  <%= link_to 'Edit your profile', edit_user_path(current_user.id) %>
<%- end %>
<br>
<%- if current_user.present? -%>
  <%= link_to 'Log out', logout_path %>
  <%= link_to 'I am here!', new_attendance_path %>
<%- else -%>
  <%= link_to 'Sign up', new_user_path %>
  <%= link_to 'Log in', new_session_path %>
<%- end %>
<h1>List of Users</h1>
<%= form_tag users_path, method: :get do %>
  <p>
    <%= date_field_tag(:date, @date) %>
    <%= submit_tag 'search' %>
  </p>
<% end %> 


<h2> Present </h2>
<table>
  <tr>
    <td>
      <% User.in_seat(1, @date).each do |s1| %>
        <%= s1.name %>
        <% if s1.image_url != "" %>
          <img src="<%= s1.image_url %>" height="80" width="80" />
        <%- else -%>
          <%= image_tag s1.gravatar_url(:default => "wavatar") %>
        <% end %>
      <% end %>
    </td>
    <td>
      <% User.in_seat(2, @date).each do |s2| %>
        <%= s2.name %>
        <% if s2.image_url != "" %>
          <img src="<%= s2.image_url %>" height="80" width="80" />
        <%- else -%>
          <%= image_tag s2.gravatar_url(:default => "wavatar") %>
        <% end %>
      <% end %>
    </td>
  </tr>
  <tr>   
    <td>
      <% User.in_seat(3, @date).each do |s3| %>
        <%= s3.name %>
        <% if s3.image_url != "" %>
          <img src="<%= s3.image_url %>" height="80" width="80" />
        <%- else -%>
          <%= image_tag s3.gravatar_url(:default => "wavatar") %>
        <% end %>
      <% end %>  
    </td>
    <td>
      <% User.in_seat(4, @date).each do |s4| %>
        <%= s4.name %>
        <% if s4.image_url != "" %>
          <img src="<%= s4.image_url %>" height="80" width="80" />
        <%- else -%>
          <%= image_tag s4.gravatar_url(:default => "wavatar") %>
        <% end %>
      <% end %> 
    </td>
  </tr>
</table>

<h3>Absent</h3>
<table>
   <tr>
     <td>
       <% User.absent(@date).each do |a| %>
         <%= a.name %>
       <% end %>
     </td>
   </tr>
</table>

